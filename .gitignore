# Secrets based on the current stack
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

/.claude/
/.next/
/out/
/.wrangler/
/archive/
/cloudflare-backups/
/node_modules
/worker/
.env*
.vercel
next-env.d.ts
*.tsbuildinfo

# Course lesson content — lives in R2 (lowlevelnotes-assets), pushed via
# `npm run content:push`. Never committed: this is the local editing
# workspace only, not a source of truth.
/content/

# dependencies
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# defaults
/coverage
/.cache/
/.obsidian/
/site/
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
